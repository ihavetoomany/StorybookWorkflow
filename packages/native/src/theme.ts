/**
 * Resurs design tokens for React Native.
 * Re-export from design-tokens so native components use the same values as web.
 * Uses ESM import so Vite/Storybook can bundle; Metro supports this for CJS packages.
 */
import theme from "@resurs-handoff/design-tokens";
export default theme;
